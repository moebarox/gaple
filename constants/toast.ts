/**
 * Default timeout for toast messages
 * without any action or callback
 */
export const DEFAULT_TOAST_TIMEOUT = 3000

/**
 * Timeout for toast messages
 * with a callback action or long text
 */
export const MEDIUM_TOAST_TIMEOUT = 5000
